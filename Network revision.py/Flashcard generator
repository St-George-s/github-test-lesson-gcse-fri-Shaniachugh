import random
terms=["LAN"]
definition=["This is the definiton of LAN"]

def start_quiz(terms, definitions):
    choice="y"
    while choice=="y":
        randomPosition=random.randint(0, len(terms)-1)
        print(terms[randomPosition])
        input("Press enter to continue")
        print(definition[randomPosition])

def showMenu():
    print("1. Start Quiz or 2. Exit")
    userInput=input("Enter options:")
    while userInput !="1" and userInput != "2":
        print("Invalid input, try again")
        userInput=input("Enter options: ")
    if userInput== "1":
        print("Quiz is starting")
    else:
        print("CYA L8r")
    
showMenu()